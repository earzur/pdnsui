<%= puts "\nDatabase configuration\n======================\n" %>

# Database configuration generated by rake db:configure
#
DB = Sequel.mysql2(
  '<%= print("Database name: "); STDOUT.flush; STDIN.gets.chomp %>', 
  :user=>'<%= print("Username: "); STDOUT.flush; STDIN.gets.chomp %>', 
  :password=>'<%= print("Password: "); STDOUT.flush; STDIN.gets.chomp %>')

